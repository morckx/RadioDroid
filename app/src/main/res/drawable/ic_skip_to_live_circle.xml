<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="?attr/colorControlNormal">
    <!-- "Skip to live edge": a single play triangle plus an end bar (jump to the end / now).
         Centered with padding to match the skip icons' footprint, and a single triangle to
         avoid looking like skip-to-next-station (which is a double triangle). -->
    <path
        android:fillColor="@android:color/white"
        android:pathData="M7,6l8,6l-8,6zM16,6h2v12h-2z" />
</vector>
